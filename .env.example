# Database — PostgreSQL (Render/Neon). Local: use a free Neon URL or Docker Postgres.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# NextAuth — generate with: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-to-a-long-random-string"

# Bootstrap admin (used by seed script only)
ADMIN_EMAIL="admin@nexus-tech.info"
ADMIN_PASSWORD="AdminChangeMe123!"

# Contact form notifications (optional)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
CONTACT_TO_EMAIL="info@nexus-tech.info"

# Telegram alerts: homepage leads, contact form, bank updates, withdrawals
# Create bot via @BotFather, get chat id via @userinfobot or getUpdates
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""
